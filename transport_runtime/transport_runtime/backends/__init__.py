"""Concrete `Backend` implementations. Import the concrete class directly
if you need it (`from transport_runtime.backends.tcp import TCPBackend`);
everything else should go through `transport_runtime.factory.get_backend()`.
"""
